# Django-Admin
Django Admin管理后台

```text
这是一个基于Django框架的管理后台项目，旨在提供一个易于使用和扩展的管理界面，方便用户对数据进行增删改查操作。
```

```shell
# 导入依赖
pip freeze > requirements.txt
```

```shell
# 运行项目
python manage.py runserver
```